DATABASE_URL="file:./dev.db"

# API URLs are injected automatically:
#   CI/CD (Amplify) → amplify.yml writes .env.local based on $AWS_BRANCH
#   Local dev       → .env.development is loaded by `npm run dev`
