# Lambda Layer – shared Python runtime dependencies for all ProjectHub functions.
# SAM installs these into the layer during `sam build`.
# Keep this file in sync with ../requirements.txt.

pydantic==2.10.3
pydantic-settings==2.7.0

# Database
pg8000==1.31.2

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
passlib==1.7.4

# AI
anthropic==0.40.0

# Utils
email-validator==2.2.0
