import { TemplatesClient } from "@/components/templates-client";

export const metadata = { title: "Templates — ProjectHub" };

export default function TemplatesPage() {
  return <TemplatesClient />;
}
