import { AnalyticsClient } from "@/components/analytics-client";

export const metadata = { title: "Analytics — ProjectHub" };

export default function AnalyticsPage() {
  return <AnalyticsClient />;
}
