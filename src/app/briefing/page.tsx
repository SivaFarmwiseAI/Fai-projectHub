import { BriefingClient } from "@/components/briefing-client";

export const metadata = { title: "Executive Briefing — ProjectHub" };

export default function BriefingPage() {
  return <BriefingClient />;
}
